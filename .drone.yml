pipeline:
  test:
    image: python:${PYTHON_VERSION}
    pull: true
    commands:
      - pip install -r requirements.test.txt
      - pip install -U "Django${DJANGO_VERSION}"
      - make test

matrix:
  DJANGO_VERSION:
    - <2.0
  PYTHON_VERSION:
    - 2.7
    - 3.5
    - 3.6
    - 3.7
